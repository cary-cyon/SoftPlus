<Window x:Class="SoftPlus.View.ManagerEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoftPlus.View"
        mc:Ignorable="d"
        Title="ManagerEdit" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Изменяемый элемент"/>
            <TextBlock Text="Имя"/>
            <TextBox Text="{Binding SelectedManager.Name}"/>
            <Button Command="{Binding EditCommand}" CommandParameter="{Binding SelectedManager}">Сохранить</Button>
        </StackPanel>
    </Grid>
</Window>
